let userName: string[] = ["Admin", "Shayan", "Adeel", "Faazi", "Hassan"];

if(userName.length === 0){
    console.log("We need to find some user!");
}
else{
    userName =[];
    console.log("All user names have been removed. " + userName.length);
}
